# Generate a long random value; never commit the real token.
KITCHEN_DISPLAY_API_TOKEN=replace-with-at-least-32-random-characters
HOST=127.0.0.1
PORT=3000
STATE_FILE=./data/state.json
PUBLIC_DIR=./dist/client
LOG_LEVEL=info
